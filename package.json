{
  "name": "qz-vercel-helper",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-polling seat helper with Vercel API proxy.",
  "scripts": {
    "check": "node --check api/proxy.js && node --check api/session.js && node --check public/script.js"
  },
  "engines": {
    "node": ">=18"
  }
}
